{
  "name": "mean-weather-news-stocks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prod": "pm2-runtime start ecosystem.config.js --env production",
    "save": "git add . && git commit -m build && git push",
    "deploy": "git add . && git commit -m Heroku && git push && git push heroku"
  },
  "author": "Daniel NGUYEN danmgs",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {},
  "cacheDirectories": []
}
